# WA Gateway — minimal production dependencies for VPS deploy.
# Do NOT use `pip freeze` to regenerate this — it pulls in editor/test deps
# and Emergent-internal packages (emergentintegrations) that don't resolve
# outside the Emergent environment.

fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.34
motor>=3.6,<4
pydantic[email]>=2.9,<3
python-dotenv>=1.0,<2
bcrypt>=4.2,<5
PyJWT>=2.9,<3
qrcode[pil]>=8,<9
Pillow>=11,<12
httpx>=0.27,<0.29
slowapi>=0.1.9,<0.2
python-multipart>=0.0.20,<1
